name: test_env_xarrayutils
channels:
  - conda-forge
dependencies:
  - python=3.7
  - xarray
  - dask
  - numpy
  - pytest
  - future
  - scipy
  - flake8
  - matplotlib
  # i would like to make this optional too
  - astropy
  # Necesary additions for the plotting tools (might have to make thos optional)
  - gsw
  - cartopy
  - xgcm
  # this is my develop env, so include these for convenience
  - black
  - pre-commit
  - pip
  - pip:
    - codecov
    - pytest-cov
