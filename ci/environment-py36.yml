name: test_env_xarrayutils
channels:
  - conda-forge
dependencies:
  - python=3.6
  - xarray < 0.13 # Testing for old xarray version
  - dask
  - numpy < 1.16
  - pytest
  - future
  - scipy
  - black
  - astropy
  - matplotlib
  # Necesary additions for the plotting tools (might have to make thos optional)
  - gsw
  - cartopy
  - xgcm
  - pip:
    - codecov
    - pytest-cov
