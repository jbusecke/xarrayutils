name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.5
  - xarray
  - dask
  - numpy
  - pytest
  - future
  - scipy
  - flake8
  - astropy
  - matplotlib
  # Necesary additions for the plotting tools (might have to make thos optional)
  - gsw
  - cartopy
  - xgcm
  - pip:
    - codecov
    - pytest-cov
